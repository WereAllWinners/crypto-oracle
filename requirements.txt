# ============================================================
# Crypto Oracle — Runtime / Inference dependencies
# For training dependencies see requirements-train.txt
# ============================================================

# Exchange connectivity
ccxt>=4.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
ta>=0.11.0                    # Technical indicators (RSI, MACD, BB, ATR)
python-dateutil>=2.8.0
pytz>=2023.0

# Macro data (DXY, VIX, SPY — no API key needed)
yfinance>=0.2.0

# Sentiment analysis
textblob>=0.17.0
praw>=7.7.0                   # Reddit (optional)
youtube-search-python>=1.6.6  # YouTube (free)

# HTTP
requests>=2.31.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Inference (CPU-only or GPU without training)
transformers>=4.40.0
torch>=2.1.0

# Environment & utilities
python-dotenv>=1.0.0
tqdm>=4.66.0

# Optional visualisation
matplotlib>=3.7.0
seaborn>=0.12.0
