# ============================================================
# Training dependencies — requires NVIDIA GPU + CUDA 12.x
# Install AFTER requirements.txt:
#   pip install -r requirements.txt
#   pip install -r requirements-train.txt
# ============================================================

# Unsloth — 2-5x faster fine-tuning with LoRA (must be first)
# Install via: pip install unsloth
# Or for latest: pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"
unsloth>=2026.2.0

# Core training stack
torch>=2.1.0
transformers>=4.40.0
trl>=0.8.0
peft>=0.10.0
datasets>=2.18.0
accelerate>=0.28.0

# 8-bit optimiser (saves VRAM during training)
bitsandbytes>=0.43.0

# Triton (required by Unsloth on Linux; skip on Windows)
# triton>=2.1.0

# Evaluation & metrics
scikit-learn>=1.3.0

# Experiment tracking (optional)
# wandb>=0.17.0
